doctype html
html
  head
    title #{clothinstance.name}
    style.
      body {
        font-family: Arial, sans-serif;
        font-size: 16px;
        color: #599;
        background-color: #f5f5f5;
      }
      h1 {
        font-size: 45px;
        margin-bottom: 20px;
        text-align: center;
      }
      a {
        color: #007bff;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      img {
        width: 200px;
        height:200px;
        display: block;
        margin: 0 auto; 
      }

  body 
    h1 #{clothinstance.name}
    img(src=clothinstance.pic_url)
    a(href=clothinstance.shopping_url) Shopping here! 